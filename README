1. Clone the repo

2. Initialize your Python virtual environment

3. Install Python dependencies

    pip install -r requirements.txt


4. Demo the model

    python run.py

IMPORTANT for non-linux users:
Visit https://pytorch.prg/get-started/locally/ and follow the prompts to install PyTorch